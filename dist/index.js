"use strict";
// Arrays
let numbers = [1, 2, 3];
let anyArray = [1, true, 'free'];
numbers.forEach((n) => n.toFixed());
// Tuples
let user = [1, 'abbas'];
user[0].valueOf();
user[1].length;
//Enums
const small = 1;
const medium = 2;
const large = 3;
let mySize = 1 /* Size.Medium */;
console.log(mySize);
